@model IEnumerable<MyFirstDatabaseCodeFirstMVC.Models.MataKuliah>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NamaMk)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.JumlahSks)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NamaMk)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.JumlahSks)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.KodeMk }) |
            @Html.ActionLink("Details", "Details", new { id=item.KodeMk }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.KodeMk })
        </td>
    </tr>
}

</table>
